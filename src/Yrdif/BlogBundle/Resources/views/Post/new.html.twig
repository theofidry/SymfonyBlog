{% extends 'YrdifBlogBundle:Post:layout.html.twig' %}

{% block body -%}
    <div class="page-header">
        <h1>New Post</h1>
    </div>

    {{ form(form) }}

    <ul class="record_actions no-list">
        <li>
            <a href="{{ path('post') }}" class="btn btn-default">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
