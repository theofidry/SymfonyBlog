{% extends 'YrdifBlogBundle:ContactRequest:layout.html.twig' %}

{% block body -%}

    <div class="page-header">
        <h1>Contact Requests</h1>
    </div>

    <table class="records_list table">
        <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Email</th>
            <th>Subject</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr onclick="window.location='{{ path('contact-request_show', { 'id': entity.id }) }}'" class="row-link vertical-align">
                <td>{{ entity.id }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.subject }}</td>
                <td>
                    <ul class="action-list">
                        <li>
                            <a href="{{ path('contact-request_show', { 'id': entity.id }) }}"
                               class="btn btn-info">
                                show
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('contact-request_edit', { 'id': entity.id }) }}"
                               class="btn btn-warning">
                                edit
                            </a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul class="no-list">
        <li>
            <a href="{{ path('contact-request_new') }}" class="btn btn-default">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}
